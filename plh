export const StyledBarChartBox = styled(Box)(({ theme }) => ({
  flex: 1,
  padding: theme.spacing(2),
  backgroundColor: theme.palette.background.paper,
  borderRadius: theme.shape.borderRadius,
  boxShadow: theme.shadows[1],
  flexDirection:"column",
  marginRight: theme.spacing(2),
  [theme.breakpoints.down("sm")]: {
    marginRight: 0,
    marginBottom: theme.spacing(2),
  },
  "& canvas": {
    maxWidth: "100%",
    height: "200px", // Decreased height for a smaller bar graph
  },
  " & .styled-diagram":{
    height: "350px", // Decreased height for the chart
    maxWidth: "100%", // Ensure the chart scales responsively
    overflow: "hidden", // Prevent the chart from overflowing its container
    display: "flex",
    flexDirection:"column",
    justifyContent: "center",
    alignItems: "center",
  },

}));


export const StyledPieChartBox = styled(Box)(({ theme }) => ({
  flex: 1,
  padding: theme.spacing(2),
  backgroundColor: theme.palette.background.paper,
  borderRadius: theme.shape.borderRadius,
  boxShadow: theme.shadows[1],
  overflow: "hidden", // Prevent content overflow
  "& canvas": {
    maxWidth: "100%",
    height: "auto",
  },
}));
